// TRIGGER - Nuclear Options - SELL IT ALL.

// For each coin type...
  // Get the current exchange rate
  // Get the current balance.
  // Get the current amount owned.
  // newAmount = Calculate the current amount owned * current excahnge rate.
  // oldAmount = previously saved newAmount saved in the DB.
  // If the newAmount is below $y (set by user) then
      // auto sell.
      // Send txt message to user.
  // Do later - If the newAmount has fallen %X (set by user) then auto sell.
  // Otherwise
    // Save the newAmount to the DB
