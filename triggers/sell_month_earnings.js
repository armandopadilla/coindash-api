// Triggers - Sell X% every month. But it must post a gain AND must be greater than $X.
// Get exchange rate
// Get coins owned.
// Get balance for each coin.
// gain = Get gains (invested - current balance)
// If gain > 200
  // Sell X%
